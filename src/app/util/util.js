(function() {
// Define the util namespace, this file is concatenated before all
// the other util files so that they can use this local var
/**
  Supplies all of the utility functions for working with arrays,
  objects, and strings.

  @alias bc.util
  @namespace
*/
var util = bc.namespace('util');
